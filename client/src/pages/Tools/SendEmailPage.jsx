import SendEmailForm from '../../components/Email/SendEmailForm';

export default function SendEmailPage() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>שליחת מייל (בדיקה / כלי אדמין)</h1>
      <SendEmailForm />
    </div>
  );
}
